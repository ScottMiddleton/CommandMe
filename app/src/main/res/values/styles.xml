<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="android:fontFamily">@string/font_default_roboto_regular</item>
        <item name="chipStyle">@style/Widget.MaterialComponents.Chip.Filter</item>
        <item name="sliderStyle">@style/SliderStyle</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="snackbarButtonStyle">@style/SnackbarActionButtonStyle</item>
    </style>

    <style name="SliderStyle" parent="Widget.MaterialComponents.Slider">
        <item name="labelBehavior">gone</item>
    </style>

    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/colorPrimaryDark</item>
    </style>

    <style name="SnackbarActionButtonStyle"  tools:override="true" parent="@style/Widget.MaterialComponents.Button.TextButton.Snackbar">
        <item name="fontFamily">@string/font_roboto_medium</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="FabButton" parent="BaseButton">
        <item name="android:backgroundTint">@color/colorPrimary</item>
        <item name="tint">@color/white</item>
    </style>

    <style name="BaseButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textAllCaps">true</item>
        <item name="android:textSize">16sp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
    </style>
    </resources>
